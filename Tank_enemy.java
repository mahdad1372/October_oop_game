import java.awt.*;

public class Tank_enemy extends Army_enemy{
    public Tank_enemy(Image img, int x, int y, int w , int h, String direction, int final_position){
        super(img, x, y, w, h, direction, final_position);
    }
}
